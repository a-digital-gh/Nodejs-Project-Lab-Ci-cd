{
  "name": "nodejs-project-lab-ci-cd",
  "version": "1.0.0",
  "description": "Production-ready Node.js API with Docker and CI/CD.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pino-http": "^10.5.0"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  }
}
